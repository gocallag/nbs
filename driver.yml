---
- hosts: localhost
  gather_facts: false
  become: false
  connection: local
  tasks:
  - name: include all the system vars
    include_vars:
        dir: "/etc/nbs/vars"
    ignore_errors: true
  - name: driver
    include_role:
       name: nbs
       tasks_from: "{{ driver }}"

